A place for us to stor all the .files to make our user environment usable.

Instructions for setup:
1: git config:
    git config --global user.name 'Name here - Location here'
    # Sets the default name for git to use when you commit
    git config --global user.email "your_email@example.com"
    # Sets the default email for git to use when you commit
    git config --global credential.helper cache
    # Set git to use the credential memory cache
    git config --global credential.helper 'cache --timeout=3600'
    # Set the cache to timeout after 1 hour (setting is in seconds)

2: Move .files into place:
    cp .vimrc ~/.vimrc
    cp config ~/.irssi/config

3: Byzantium:

4: Paparrazzi:

TODO:
add section on Byzantium
add section on Paparazzi
rework README in whatever code Nathen uses for his READMEs

